import javafx.application.Application;

public class App {
    /**
     * Drives the program
     * 
     * @param args
     */
    public static void main(String[] args) {
        Application.launch(GUI.class, args); // Start the GUI and the Game
    }
}
